(function () {
    angular.module('TodoCalls', [
        'smart-table',              // for list of calls
        'TodoCalls.directives',     // planned for list items
        'TodoCalls.controllers',
        'TodoCalls.services'
    ]);
})();
